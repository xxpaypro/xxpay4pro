package org.xxpay.service.nc.repository;

/*
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.Sort;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.CrudRepository;
import org.springframework.data.web.PageableDefault;
import org.springframework.stereotype.Component;
import org.xxpay.service.nc.model.Street;

import java.util.List;
*/

//@Component
//public interface StreetRepository extends CrudRepository<Street, String>{
public interface StreetRepository {

    /*
    Street findByCode(String code);

    List<Street> findByAreaCode(String areaCode);

    Page<Street> findByAreaCode(@PageableDefault(value = 10, sort = {"areaCode"}, direction = Sort.Direction.DESC) Pageable pageable, String areaCode);

    Page<Street> findAll(@PageableDefault(value = 10, sort = {"cityCode"}, direction = Sort.Direction.DESC) Pageable pageable);
    */
}
