package org.xxpay.service.nc.repository;

/*
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.Sort;
import org.springframework.data.repository.CrudRepository;
import org.springframework.data.web.PageableDefault;
import org.springframework.stereotype.Component;
import org.xxpay.service.nc.model.Village;

import java.util.List;
*/

//@Component
//public interface VillageRepository extends CrudRepository<Village, String> {
public interface VillageRepository {

    /*
    Village findByCode(String code);

    List<Village> findByStreetCode(String streetCode);

    Page<Village> findByStreetCode(@PageableDefault(value = 10, sort = {"streetCode"}, direction = Sort.Direction.DESC) Pageable pageable, String streetCode);

    Page<Village> findAll(@PageableDefault(value = 10, sort = {"streetCode"}, direction = Sort.Direction.DESC)Pageable pageable);
    */

}
